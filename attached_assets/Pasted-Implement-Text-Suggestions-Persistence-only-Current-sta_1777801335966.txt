Implement Text Suggestions Persistence only.

Current state:
- Real AI runtime is wired for campaign workflow stages.
- Stage 2 is now “تكييف الاستراتيجية للحملة”, not full brand strategy.
- Stage 3 text suggestions currently use the existing text-assist path but are transient and lost on refresh.
- This is not acceptable for a real working product.

Do not redesign UI.
Do not touch Dashboard.
Do not touch Brand UI.
Do not touch Content UI.
Do not touch Review UI.
Do not implement upload.
Do not implement image generation.
Do not implement video generation.
Do not implement live publishing.
Do not implement payments.
Do not implement autonomous optimization.
Do not change Campaign Completion logic.
Do not change manual publish guards.
Do not expose API keys in frontend.
Do not change OpenAPI or generated client unless strictly required.

Objective:
Make Stage 3 “تجهيز المحتوى” persist generated text suggestions so they reload after refresh and behave like real draft campaign outputs, not temporary UI-only results.

Scope:
Persist only text suggestions generated by Stage 3.

Required behavior:

1. Persistence target review
First inspect existing schema and models for any suitable storage:
- campaign ads
- content drafts
- generated content
- workflow artifacts
- ad variants
- campaign-related text output

If a suitable existing table exists, use it.
If no suitable table exists, create the smallest appropriate persistence model consistent with the current architecture.

Do not create unnecessary broad content management tables.

2. Data to persist
Persist the full Stage 3 output:
- hooks
- adCopyVariants
- captions
- ctas
- improvementNotes
- missingContextWarnings
- safetyNotes
- campaignId
- workspaceId
- source: real | mock | unavailable
- status: draft
- generatedBy user id if available
- createdAt / updatedAt if pattern exists

3. Draft-only governance
Persisted text suggestions must remain draft-only.
They must not:
- approve content
- create approved ads
- mark campaign ready
- alter readiness score directly
- publish
- modify budget
- update assets

4. Frontend reload behavior
The redesigned AI Workflow Stage 3 must:
- show saved text suggestions after page refresh
- show draft-only state
- allow regeneration if existing behavior supports it
- not lose results after reload

5. Role and workspace guards
All write operations must:
- require authenticated user
- require editor or above
- validate campaign belongs to workspace
- deny viewer generation

Read-only access should follow existing workspace role policy.

6. Missing key behavior
If OPENAI_API_KEY is missing:
- do not save fake content as real
- return safe unavailable state
- UI shows AI unavailable
- no mock success in production

7. Mock behavior
If mock is used in dev/local:
- persisted record must include source: "mock"
- production must not persist mock as successful real content

8. Documentation
Create:
docs/text_suggestions_persistence_report.md

Update if needed:
docs/real_ai_workflow_runtime_persistence_report.md

Report must include:
- changed files
- selected persistence target
- why it was chosen
- schema changes if any
- route/action changes
- frontend reload behavior
- draft-only governance
- role/workspace guards
- missing-key behavior
- mock behavior
- verification results
- remaining gaps
- readiness decision

Verification:
- TypeScript zero errors
- API server build passes
- frontend build passes:
  PORT=3000 BASE_PATH=/ pnpm --filter @workspace/marketing-os run build
- generated text suggestions persist
- saved suggestions reload after refresh
- viewer cannot generate
- editor can generate
- no API key in frontend
- no frontend OpenAI/provider call
- no image generation
- no video generation
- no upload
- no live publishing
- no Campaign Completion regression

Final output:
- changed files
- text persistence implemented
- reload behavior verified
- verification results
- remaining gaps
- readiness decision